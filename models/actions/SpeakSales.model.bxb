action (SpeakSales) {
  description (정산 내역을 말해준다.)
  // 정산내역 % 말해줘
  type(Search)
  collect {
    input (salesKeyword) {
      type(SalesKeyword)
      min (Required)
    }
    input (request) {
      type(Request)
      min(Required) max (One)
    }
  }
  output (SalesList)
}