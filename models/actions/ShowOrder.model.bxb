action (ShowOrder) {
  description (주문 내역을 화면에 보여준다)
  type(Search)
  output (OrderList) {
    on-empty{
      halt{
        dialog{
          template ("현재 주문이 없습니다.")
        }
      }
    }
  }
}