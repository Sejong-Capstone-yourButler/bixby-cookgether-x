action (ShowOrder) {
  description (주문 내역을 화면에 보여준다)
  // 주문내역 % 시간순으로 % 알려줘
  type(Search)
  collect {
    input (orderKeyword) {
      type(OrderKeyword)
      min (Required)
    }
    input (align) {
      type (SortingOption)
      min (Optional) max (One)
    }
    input (request) {
      type(Request)
      min(Required) max (One)
    }
  }

  output (OrderResult) {
    on-empty{
      halt{
        dialog{
          template ("현재 주문이 없습니다.")
        }
      }
    }
  }
}
