action (ShowStock) {
  description (__DESCRIPTION__)
  type(Search)
  collect {
    input (menuname) {
      type (MenuName)
      min (Required)
    }
  }
  output (StockList) {
    on-empty{
      halt{
        dialog{
          template ("")
        }
      }
    }
  }
}