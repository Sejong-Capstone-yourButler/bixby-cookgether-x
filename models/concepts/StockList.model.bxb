structure (StockList) {
  description (재고 내역)
  property (name) {
    type (StockName)
    min (Required) max(Many)
  }
  property (num) {
    type (StockNum)
    min (Required) max(Many)
  }
}
